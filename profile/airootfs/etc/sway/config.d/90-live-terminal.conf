# Live-ISO only: open a terminal on startup so the installer (kkjorsvik-install)
# is reachable without relying on Super+Return — which doesn't pass through some
# VM consoles (e.g. Proxmox noVNC). The installer does NOT copy /etc/sway/config.d/,
# so installed systems do not get this auto-terminal.
exec foot
